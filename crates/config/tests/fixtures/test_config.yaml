server:
  host: localhost
  port: 8080
  debug: true

database:
  url: "postgres://localhost/nebula"
  pool_size: 10

logging:
  level: info
  targets:
    - stdout
    - file

features:
  - auth
  - webhooks
  - metrics
