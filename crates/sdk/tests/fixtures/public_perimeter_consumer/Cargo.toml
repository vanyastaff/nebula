[package]
name = "nebula-sdk-public-perimeter-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
async-trait = "0.1.89"
nebula-sdk = { version = "=0.7.0", default-features = false }

[workspace]

[package.metadata.nebula.fixture]
test-target = "public_perimeter_external_contract"
