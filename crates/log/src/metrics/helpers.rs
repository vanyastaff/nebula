//! Metrics helpers — moved to `nebula-telemetry` / `nebula-metrics`.
//!
//! This module previously provided `timed_block`, `timed_block_async`, and
//! `TimingGuard` backed by the ecosystem `metrics` crate. Those utilities now
//! live in the dedicated metrics pipeline crates.
