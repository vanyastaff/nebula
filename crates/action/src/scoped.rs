//! Scoped credential accessor — re-exported from [`nebula_credential`].
//!
//! The canonical implementation lives in [`nebula_credential::accessor`].
//! This module exists only as a re-export point for backward compatibility.

pub use nebula_credential::ScopedCredentialAccessor;
