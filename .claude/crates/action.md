# nebula-action
Action trait hierarchy and execution contract — Ports & Drivers architecture.

## Invariants
- Defines **what** actions are, not how the engine runs them. Concrete execution environments are in nebula-runtime.
- Actions may run concurrently. No mutable state in action struct fields — use `StatefulAction` for stateful loops.
- `Context` is always injected — never construct `ActionContext` directly inside an action.

## Key Decisions
- Action subtypes: `StatelessAction` (one-shot), `StatefulAction` (Continue/Break loop), `TriggerAction` (starts workflow), `ResourceAction` (branch-scoped DI setup/cleanup, single associated `type Resource`).
- `ActionDependencies` has two complementary pairs: `credential`/`resources` (trait-object, runtime injection) and `credential_keys`/`resource_keys` (typed keys, engine validation at registration). Plus `credential_types()` → `Vec<TypeId>` for `ScopedCredentialAccessor` sandboxing. All five default to empty — no migration needed.
- `#[derive(Action)]` with `#[action(credential = T)]` or `#[action(credentials = [T1, T2])]` generates both `credential()` and `credential_types()`. Duplicate credential types in the attribute produce a compile error.
- `ActionRegistry` lives in **`nebula-runtime`** (Phase 7.5), not in `nebula-action`. The protocol crate stays free of execution concerns.
- `credential::<S>()` is the primary typed credential access path (returns `CredentialGuard<S>`). Renamed from `credential_by_type::<S>()` in Phase 2b (deprecated alias kept). `credential_by_id(id)` for string-based access (old `credential(id)` removed — name clash). `has_credential_id(id)` replaces `has_credential(id)` (deprecated alias kept). Legacy `credential_typed::<S>(id)` (string-based, consumes snapshot via `into_project::<S>()`, maps `SnapshotError` → `ActionError::Fatal`) preserved for backward compat.
- `RetryHintCode` enum (8 variants, `#[non_exhaustive]`, formerly `ErrorCode`) — retry-strategy hint attached by the action body to `Retryable`/`Fatal` (RateLimited, AuthExpired, UpstreamTimeout, etc.). Renamed 2026-04-10 to disambiguate from `nebula_error::Classify::code()` which is the cross-crate taxonomy tag. Accessor: `ActionError::retry_hint_code() -> Option<RetryHintCode>` returns by value (enum is `Copy`). Constructors/Ext methods: `retryable_with_hint` / `fatal_with_hint`.
- `ActionError::Validation` is a struct variant `{ field: &'static str, reason: ValidationReason, detail: Option<String> }` (L7, 2026-04-10). `field` MUST be a compile-time constant — type-level guarantee that the always-logged part of the error cannot carry user input. `detail` is sanitized at construction (`sanitize_detail` escapes control chars as `\uXXXX`, truncates to `MAX_VALIDATION_DETAIL = 256` bytes with `…` marker) — defeats log injection via newlines/ANSI/null bytes. Constructor: `ActionError::validation(field, reason, detail: Option<impl Into<String>>)`. Pattern-match as `ActionError::Validation { field, reason, detail }`, not `Validation(s)`. `ValidationReason`: MissingField / WrongType / OutOfRange / MalformedJson / StateDeserialization / Other, with `as_str()` returning stable snake_case tags for metrics.
- `ActionErrorExt` trait (in `error.rs`) — `.retryable()?` and `.fatal()?` ergonomic conversion on any `Result<T, E>`. Also `_with_code()` variants for ErrorCode attachment. Renamed from `ActionResultExt` on 2026-04-10 — name reflects that the trait produces `ActionError`, not `ActionResult`.
- Error field: `Arc<anyhow::Error>` — preserves full error chain, Clone via Arc. Factory methods accept `impl Display + Debug + Send + Sync + 'static`.

- `CredentialGuard<S: Zeroize>` — the **only** credential type re-exported from `nebula-action` (via `pub use nebula_credential::CredentialGuard` at `lib.rs`). Deref + Zeroize on drop + !Serialize. Constructed in context methods via `CredentialGuard::new()`.
- `credential::<S>()` on `ActionContext`/`TriggerContext` — type-based credential access via TypeId. Returns `CredentialGuard<S>`.
- `CredentialAccessor`, `ScopedCredentialAccessor`, `NoopCredentialAccessor`, `CredentialAccessError`, `default_credential_accessor` — NOT re-exported from `nebula-action` (purged 2026-04-10). Import directly from `nebula_credential` if building an `ActionContext` manually. Internally, `context.rs` / `testing.rs` use `nebula_credential::{CredentialAccessor, ...}` directly. `From<CredentialAccessError> for ActionError` still exists in `error.rs` and maps `AccessDenied` → `SandboxViolation`, others → `Fatal`.
- `#[derive(Action)]` now works on structs with fields (not just unit structs) — enables `type Input = Self` pattern.

- `ActionHandler` enum (5 variants: Stateless, Stateful, Trigger, Resource, Agent, `#[non_exhaustive]`) — engine match-dispatches. Replaces removed `InternalHandler`. Derives `Clone` (all variants are `Arc<dyn ...>`).
- 5 handler traits: `StatelessHandler`, `StatefulHandler`, `TriggerHandler`, `ResourceHandler`, `AgentHandler` (stub).
- 4 adapters: `StatelessActionAdapter`, `StatefulActionAdapter`, `TriggerActionAdapter`, `ResourceActionAdapter` — bridge typed traits to JSON-erased handlers.
- `ActionRegistry` stores `ActionHandler`, has typed `register_stateless/stateful/trigger/resource` convenience methods.
- `StatefulAction::State` now requires `Serialize + DeserializeOwned + Clone`. `init_state()` is required. `StatefulHandler::init_state()` returns `Result<Value, ActionError>` (fallible).

## Testing Infrastructure (Phase 5)
- Assertion macros (`assert_success!`, `assert_branch!`, etc.) via `#[macro_export]` — match on real `ActionResult`/`ActionError` variants. Import via `use crate::assert_*` in tests.
- `TestContextBuilder`: `minimal()`, `with_credential_snapshot()`, `with_credential::<S>()` (type-based), `with_resource()`, `with_input()`, `build_trigger()`.
- `StatefulTestHarness<A>`: wraps `StatefulAction`, serializes/deserializes state between `step()` calls, exposes `state::<S>()` and `state_json()`.
- `TriggerTestHarness<A>`: wraps `TriggerAction` with `SpyEmitter`/`SpyScheduler`, exposes `emitted()`, `scheduled()`, `start()`/`stop()`.
- `SpyEmitter` / `SpyScheduler`: test doubles capturing `emit()` and `schedule_after()` calls.
- `TestResourceAccessor` uses `remove()` — a resource can only be acquired once per test (mirrors real acquire semantics).

## Module layout (reorganised 2026-04-11)

Every domain lives in one flat file. The former 2294-line `handler.rs`
mega-file was split: each `{Stateless,Stateful,Trigger,Resource}Handler`
trait now lives next to the typed trait and adapter it bridges. Webhook
and poll are peers of `trigger.rs` — different lifecycle models
(push+register vs pull+interval).

- `action.rs` — base `Action` trait (identity + metadata). 21 lines.
- `stateless.rs` — `StatelessAction` trait + `FnStateless*` function-backed DX adapters + **`StatelessHandler` dyn trait + `StatelessActionAdapter`** + all stateless adapter tests.
- `stateful.rs` — `StatefulAction` trait + `PaginatedAction` / `BatchAction` DX + `impl_paginated_action!` / `impl_batch_action!` macros + **`StatefulHandler` dyn trait + `StatefulActionAdapter`** + A1 checkpoint tests (`CounterAction`, `MutateThenFailAction`). Macros generate `impl StatefulAction for $ty` — no blanket impls (coherence). **`TransactionalAction` and `impl_transactional_action!` were deleted 2026-04-10** (M1) — three-phase saga was unreachable past the first phase.
- `trigger.rs` — base `TriggerAction` trait + **`TriggerHandler` dyn trait + `TriggerActionAdapter`** + `IncomingEvent` + `TriggerEventOutcome` + `DEFAULT_MAX_BODY_BYTES` + `MAX_HEADER_COUNT`. Webhook / poll specializations live in their own peer files.
- `webhook.rs` — `WebhookAction` DX trait + `WebhookTriggerAdapter` + HMAC signature primitives (`verify_hmac_sha256`, `hmac_sha256_compute`, `verify_tag_constant_time`, `SignatureOutcome`). Constant-time via `subtle::ConstantTimeEq`.
- `poll.rs` — `PollAction` DX trait + `PollTriggerAdapter` + `WarnThrottle` + `StartedGuard` (defused RAII) + `POLL_INTERVAL_FLOOR` (100 ms) + `POLL_WARN_COOLDOWN` (30 s). Poll-internal state (throttles, guard) is module-private.
- `resource.rs` — `ResourceAction` DX trait + **`ResourceHandler` dyn trait + `ResourceActionAdapter`** + roundtrip tests. Single `type Resource` after A4 collapse.
- `handler.rs` — dispatcher-only: `ActionHandler` enum + `is_*` predicates + Debug + `AgentHandler` stub trait + shared `Test*Handler` stubs for cross-variant tests. Handler traits for the other four variants live in their domain files (`stateless.rs`, `stateful.rs`, `trigger.rs`, `resource.rs`) and are reached through the crate root (`nebula_action::StatelessHandler`, etc.). There are no `handler::X` alias re-exports — the Apr-11 post-audit cleanup deleted them.
- `error.rs` — `ActionError` + `RetryHintCode` (renamed from `ErrorCode` in M2) + `ActionErrorExt` (merged from old `ext.rs`).
- `result.rs` — `ActionResult` variants, `BranchKey`, `BreakReason`, `PortKey`, `WaitCondition`, `continue_with()`, `break_completed()`, etc.

- `migrate_state(old: Value) -> Option<Self::State>` — default method on `StatefulAction`. Adapter calls on state deser failure. Returns `None` by default (error propagated).
- `ActionResult::continue_with()`, `break_completed()`, `break_with_reason()`, `continue_with_delay()` — convenience constructors for stateful iteration results.
- `IncomingEvent` — transport-agnostic event struct (body bytes + headers map + source). Lives in `trigger.rs` since it appears in `TriggerHandler::handle_event` signature. Construction is fallible via `try_new` / `try_new_with_limits` — enforces size and header-count limits at the type boundary. Canonical import is `nebula_action::IncomingEvent` (crate root).
- `TriggerEventOutcome` enum (Skip/Emit/EmitMany) on `TriggerHandler` — universal event ingress. `accepts_events()` + `handle_event()` with default error. `handle_event` takes typed `IncomingEvent` directly (NOT `Value`) — no JSON round-trip, no body bloat.
- `WebhookTriggerAdapter` stores state as `RwLock<Option<Arc<State>>>`. `handle_event` clones the `Arc` under read lock and releases BEFORE await (prevents deadlock with concurrent start/stop). `handle_event` before `start()` returns Fatal error.
- `PollTriggerAdapter::start()` blocks in a `tokio::select!` loop until cancellation. Fatal errors stop the loop, Retryable errors skip the cycle, emit failures silently dropped.

- **Phase 7.5 (2026-04-09):** `ActionRegistry` moved to `nebula-runtime` (was in `nebula-action`). Protocol crate stays free of execution concerns and `dashmap` dependency. `use nebula_runtime::ActionRegistry`.
- **`StatefulHandler::execute` borrows input** (`&Value`, not owned `Value`) — runtime stateful loop reuses input across iterations without per-iteration cloning. Adapter clones once internally for typed deserialization. User-level `StatefulAction::execute` is unchanged.
- **`InternalHandler` deleted entirely.** All execution flows through `ActionHandler` enum dispatch in `nebula_runtime::ActionRuntime::run_handler`.
- **`ActionHandler` enum is now `Clone`** — all variants wrap `Arc<dyn ...>`, so cloning is cheap pointer copies. Required for owned-tuple lookups from the registry.

## Traps
- `ActionError::retryable(...)` vs `ActionError::fatal(...)` — engine uses this to decide retry. Use `ActionErrorExt` for ergonomic `.retryable()?` / `.fatal()?`.
- `FnStatelessAction` / `stateless_fn()` for closure-based actions (testing and one-off use). `FnStatelessCtxAction` / `stateless_ctx_fn()` for closures that need `ActionContext` (credentials, resources, logger). Use `.with_context(ctx)` to inject capabilities.
- `CredentialGuard` does NOT impl Serialize — compile error if put in Output/State types. By design.
- `InternalHandler` was deleted in Phase 7.5. Use `ActionHandler` enum exclusively.
- `ResourceActionAdapter::cleanup` downcasts `Box<dyn Any>` — returns `ActionError::Fatal` on type mismatch during cleanup downcast.
- Must call `impl_paginated_action!(MyType)` after `impl PaginatedAction for MyType` — the macro generates the `StatefulAction` impl. Forgetting the macro = type won't work with `register_stateful()`.
- A type cannot use two DX macros (e.g., both `impl_paginated_action!` and `impl_batch_action!`) — duplicate `StatefulAction` impl error. Choose one pattern per type.
- `BatchAction::process_item` returning `ActionError::Fatal` aborts the entire batch. Use `ActionError::Retryable` for per-item errors that should be captured and continued.
- `PollTriggerAdapter::start()` blocks until cancellation — engine MUST spawn it in a task. Tests use `#[tokio::test(start_paused = true)]` + `tokio::time::advance` + `yield_now` for determinism. Requires `tokio` `test-util` feature.
- `StatefulActionAdapter::execute` MUST checkpoint typed_state back to the JSON state on BOTH `Ok` and `Err` paths before propagating. A Retryable with mutated cursor/counter must be flushed, or the engine replays completed work on retry — duplicated API calls, double charges, double emits. The only exception is Validation raised during input/state deserialization (typed_state never existed). If state serialization itself fails on the error path, the adapter logs via `tracing::error!` and propagates the original action error — masking it would break retry classification.
- `WebhookTriggerAdapter::start` rejects double-start with `ActionError::Fatal` — silent re-registration would leak external webhook registrations (GitHub/Slack/Stripe stay live while only the latest one gets `stop()`d). Sequential double-start is caught by a read-lock pre-check before `on_activate` runs; concurrent race is caught by a write-lock re-check that tears down the orphan state via `on_deactivate`. Guard is held entirely inside a scoped block so it cannot sit across `.await` — non-Send parking_lot guards across suspension points would make the whole future `!Send`.
- `PollTriggerAdapter::start` rejects double-start via `AtomicBool` sentinel + `compare_exchange(AcqRel, Acquire)`. Sentinel is cleared by a `StartedGuard` RAII drop (defused pattern — NO `mem::forget`) so the flag is cleared on every exit path (cancellation, error, normal return). Two concurrent `start()` calls would otherwise share the adapter's cursor and double-emit every event.
- `PollTriggerAdapter` clamps `poll_interval()` to `POLL_INTERVAL_FLOOR = Duration::from_millis(100)`. Shorter intervals are a busy-loop bug or an abusive integration; legitimate high-frequency event sources should use streaming triggers (post-v1). The clamp emits a one-shot warn line via `ctx.logger` (not `tracing::warn!`) at the start of `start()` so the divergence from the action's declared interval is visible in the same logging pipeline as the rest of the adapter's per-cycle warnings. Tests that need to exercise the adapter must use ≥100 ms intervals to avoid fighting the clamp. Silent error drops in the poll loop (retryable poll error, event serialization failure, emit failure) also log via `ctx.logger` with per-kind `WarnThrottle` (30 s cooldown) — see PR 8 / M6 / Apr-11 refactor.
- `TriggerHandler::start` has TWO valid shapes: (1) **setup-and-return** (webhook) registers a listener and returns immediately; (2) **run-until-cancelled** (poll) runs the entire loop inline and returns only on cancellation or fatal error. Callers MUST spawn `start()` in a dedicated task — calling sites that sequentially `await` multiple triggers deadlock on the first shape-2 trigger. Double-start on either shape returns `Fatal` — no silent re-registration.
- `WebhookTriggerAdapter::handle_event` before `start()` returns `ActionError::Fatal` — webhook layer must ensure trigger is started before routing events.
- `IncomingEvent` lives in `trigger.rs`. Canonical import is `nebula_action::IncomingEvent` (crate root re-export). The `nebula_action::handler::IncomingEvent` alias was removed in the Apr-11 re-export purge; use the crate-root path.
- `IncomingEvent` construction is fallible: `try_new(body, headers)` enforces `DEFAULT_MAX_BODY_BYTES` (1 MiB) and `MAX_HEADER_COUNT` (128). `try_new_with_limits(body, headers, max_body, max_headers)` for providers that need larger caps (e.g., GitHub release events up to 25 MB). Returns `DataLimitExceeded` on oversized body, `Validation` on too-many-headers. Oversize intake must be a deliberate, code-reviewed decision.
- `IncomingEvent` header keys are lowercased at construction — `header()` is O(1) regardless of caller case. Fast path: all-lowercase keys skip the allocation. Duplicate keys collapse to the last value; consumers needing multi-valued header semantics must parse them from the underlying transport before constructing the event.
- `ctx.cancellation` and `ctx.emitter` accessed as pub fields in PollTriggerAdapter — known tech debt, should be methods. Tracked for TriggerContext refactor.
- `PollAction::Cursor` is in-memory only — resets to `Default` on every `start()`. Cross-restart persistence requires runtime storage integration (post-v1).
- `ActionRegistry` is in `nebula-runtime`, NOT `nebula-action`. Importing from `nebula_action::ActionRegistry` will fail to compile.

## Relations
- Depends on nebula-core, nebula-parameter, nebula-credential. Used by nebula-engine, nebula-runtime, nebula-sdk.

<!-- reviewed: 2026-04-10 — module layout cleanup: execution.rs/authoring.rs/ext.rs/scoped.rs deleted; stateless/trigger/resource split into own files; ActionResultExt → ActionErrorExt in error.rs; credential re-exports purged (only CredentialGuard remains in public API) -->
<!-- reviewed: 2026-04-10 — A1: StatefulActionAdapter::execute checkpoints state on both Ok and Err paths; serde failure on error path logs via tracing and propagates original action error. StatefulHandler::execute doc updated with "State checkpointing" invariant. Added tracing as direct dep. -->
<!-- reviewed: 2026-04-10 — A2: WebhookTriggerAdapter::start rejects double-start with Fatal (read-lock pre-check + write-lock re-check with orphan deactivation). PollTriggerAdapter::start uses AtomicBool sentinel + StartedGuard RAII (defused pattern). TriggerHandler::start doc rewritten to declare dual contract (setup-and-return vs run-until-cancelled) and mandate task spawning. -->
<!-- reviewed: 2026-04-10 — A3: POLL_INTERVAL_FLOOR (100 ms) clamps configured poll_interval; busy-loop prevention. Silent error drops in PollTriggerAdapter replaced with tracing::warn!/debug! (temporary — Phase 5 migrates to ctx.logger + WarnThrottle). -->
<!-- reviewed: 2026-04-10 — A4: ResourceAction collapsed to single associated `type Resource` (was Config + Instance). ResourceActionAdapter downcast is now a true invariant — mismatch is an engine bug, not a user footgun. `nebula_runtime::ActionRegistry::register_resource` lost its redundant A::Config/A::Instance bounds. New integration test: resource_roundtrip. -->
<!-- reviewed: 2026-04-10 — M2: ErrorCode enum → RetryHintCode (disambiguates from nebula_error::Classify::code() framework taxonomy tag). ActionError::error_code() → retry_hint_code() returning Option<RetryHintCode> by value. retryable_with_code/fatal_with_code → *_with_hint. ActionErrorExt methods also renamed. Zero non-test callers in workspace — clean rename. -->
<!-- reviewed: 2026-04-10 — A5: new module `nebula_action::webhook` with constant-time HMAC-SHA256 verification (verify_hmac_sha256, SignatureOutcome, hmac_sha256_compute, verify_tag_constant_time). Closes the timing side-channel that the previous doc example led authors to write. Fixed misleading doc example in trigger.rs. Added hmac/sha2/hex/subtle as direct deps; subtle newly added to workspace. 11 new integration tests in tests/webhook_signature.rs. -->
<!-- reviewed: 2026-04-10 — M4+M5: IncomingEvent::new removed in favor of try_new/try_new_with_limits. DEFAULT_MAX_BODY_BYTES = 1 MiB, MAX_HEADER_COUNT = 128. Header keys normalized to lowercase at construction; header() fast path for all-lowercase queries (no allocation), slow path for mixed case (single fold). Duplicate keys collapse to last value (documented). 12 new integration tests in tests/incoming_event_limits.rs. Breaking: 3 test call sites in the crate migrated. -->
<!-- reviewed: 2026-04-10 — M1: TransactionalAction + TransactionPhase + TransactionState + impl_transactional_action! macro deleted. examples/transactional.rs + tests/dx_transactional.rs deleted. sdk::prelude re-exports cleaned up. Rationale documented in .claude/decisions.md: three-phase state machine was unreachable past Pending in actual engine dispatch, Executed/Compensated branches were dead code. Saga orchestration is a post-v1 engine feature — will ship with its own trait shape when real rollback + saga state store + compensation DAG land. -->
<!-- reviewed: 2026-04-10 — L7: ActionError::Validation(String) restructured into Validation { field: &'static str, reason: ValidationReason, detail: Option<String> }. `field` is compile-time constant by type (cannot carry user input). `detail` is sanitized at construction: control chars escaped as \uXXXX, truncated to MAX_VALIDATION_DETAIL (256 B) with '…' marker. ValidationReason enum: MissingField / WrongType / OutOfRange / MalformedJson / StateDeserialization / Other, with as_str() for metrics. Closes log-injection footgun where serde_json::Error messages were passed verbatim. All call sites migrated (handler.rs input/state, webhook.rs empty secret, IncomingEvent header count, test assert_validation_error! macro, dx_webhook.rs body parse). -->
<!-- reviewed: 2026-04-10 — PR 8 (M3+M6+L1-L6): (M6) PollTriggerAdapter retryable/emit/serialize errors now log via ctx.logger (ActionLogger) with per-kind WarnThrottle (30s cooldown) — replaces the temporary tracing::warn! from A3; three throttle fields added to adapter struct. (M3) TestResourceAccessor stores `Arc<dyn Fn() -> Box<dyn Any>>` factories instead of single Box — multi-acquire for same key now works, matches production semantics. (L1) duration_ms serializer uses u64::try_from instead of `as u64` (saturating, not wrapping). (L2) ActionResult::continue_with / continue_with_delay clamp progress fraction to 0.0..=1.0 with NaN → 0.0 via sanitize_fraction helper. (L3) BinaryData::effective_size() method returns actual Inline bytes.len() or advertised Stored size — size-limit checks MUST use this to avoid bypass via falsified size field. (L4) DeferredRetryConfig::validate() rejects non-finite / ≤0 backoff_coefficient and zero max_attempts before the resolver's Duration::mul_f64 panics on them. (L5) WebhookTriggerAdapter::handle_event race documented: stop() vs in-flight handle_request may briefly hold two Arc<State> clones side by side; benign for typical on_deactivate shapes, authors that tear down shared resources in on_deactivate must coordinate externally. (L6) impl_paginated_action! adds debug_assert!(max_pages >= 1) inside the generated execute — release still coerces to 1. -->
<!-- reviewed: 2026-04-10 — M7: Extracted `CredentialContextExt` trait with default method implementations for credential_by_id / credential_typed / credential / has_credential_id. Implemented for both ActionContext and TriggerContext via a single `credentials() -> &Arc<dyn CredentialAccessor>` method. Removed ~130 LOC of duplicated inherent impl methods from both contexts. Trait is in prelude so most callers don't need explicit import. Callers outside prelude glob-import (nebula-runtime, nebula-engine tests) needed one-line `use nebula_action::context::CredentialContextExt;`. Deprecated `credential_by_type` / `has_credential` removed along with the deduplication (zero non-deprecated callers). -->
<!-- reviewed: 2026-04-10 — PR 10 polish bundle (Q1+Q3+Q4+Q7/Q10/M1-M3+Q8+Q9+Q12+Q18+Q19+M6+R1+R2): API Guidelines hygiene pass. Debug impls on 6 previously-bare adapter types. non_exhaustive on FlowKind/InputPort/OutputPort/DeltaFormat/3 error enums (Overflow excluded — runtime matches exhaustively). ActionPackageValidationErrors.errors made private with errors() getter. must_use applied across ~30 constructors/builders/predicates on ActionMetadata/ActionError/ActionResult. Q12: into_primary_value deleted (callers chain into_primary_output().and_then). Q4: webhook primitives re-exported at crate root and prelude. Q8: CredentialContextExt forward-compat contract documented. Q19: fixed intra-doc link ScopedCredentialAccessor. M6: Arc::unwrap_or_clone (stable 1.76). R2: webhook rollback on_deactivate error now logs via tracing::warn! instead of silent drop. R1: webhook HMAC .expect() sites gained #[allow(clippy::expect_used)] + // Reason: RFC 2104 comment (HMAC construction is structurally infallible). -->
<!-- reviewed: 2026-04-10 — PR 12 (Q2): ActionMetadata derives PartialEq + Serialize + Deserialize. ParameterCollection, InterfaceVersion, InputPort, OutputPort, IsolationLevel, ActionKey all already impl these — the derive cascaded cleanly on the first try, no dep changes needed. Two new tests: metadata_partial_eq_roundtrip (structural equality for dedup/cache-key), metadata_serde_roundtrip (JSON persistence for engine state + cross-process plugin discovery). -->
<!-- reviewed: 2026-04-10 — PR 13 (M4+M5): StatefulActionAdapter::execute nested match flattened into a 2D tuple match on (serialize result, action result) — identical semantics, easier to audit on the checkpoint-critical path. PollTriggerAdapter poll-loop body extracted into private `dispatch_events(events, ctx)` helper — the tokio::select! arms are now flat. TriggerHandler::start trait doc gained a full `# Cancel safety` section explaining the dual-shape contract, the drop-safety requirement, and pointing at PollTriggerAdapter::start as the worked example for shape-2 implementations. No behavior change. -->
<!-- reviewed: 2026-04-11 — Domain-grouping refactor: handler.rs 2294-line mega-file split into six peer files (stateless/stateful/trigger/webhook/poll/resource). Each domain owns its core trait + DX adapter + dyn Handler trait + typed adapter + tests. handler.rs shrank to ~400 lines holding only the ActionHandler enum + AgentHandler stub + shared Test*Handler stubs + cross-variant tests, plus alias-surface re-exports of every moved type so `nebula_action::handler::X` paths keep resolving (load-bearing for runtime/sdk/engine/integration tests). New poll.rs file; webhook.rs doubled in size to host trait+adapter alongside existing HMAC primitives. IncomingEvent + DEFAULT_MAX_BODY_BYTES + MAX_HEADER_COUNT relocated from handler.rs to trigger.rs (the type is in TriggerHandler::handle_event signature). WarnThrottle / StartedGuard / POLL_INTERVAL_FLOOR / POLL_WARN_COOLDOWN moved into poll.rs as module-private. Zero behavior change; 417/417 tests green. sdk runtime.rs + sdk prelude.rs imports flipped from trigger:: to poll:: / webhook:: for PollAction / WebhookAction. -->
<!-- reviewed: 2026-04-11 — Re-export purge: all 14 `pub use crate::{stateless,stateful,trigger,webhook,poll,resource}::X` deleted from `handler.rs`. Canonical public path is `nebula_action::X` (crate root), not `nebula_action::handler::X`. Callers migrated: nebula-sdk `runtime.rs`, nebula-sandbox `discovery.rs` + `handler.rs`, nebula-cli `commands/actions.rs` (three inline `&dyn` types), and five nebula-action integration tests. `handler.rs` now really is dispatcher-only: `ActionHandler` enum, `AgentHandler` stub, Test*Handler stubs, cross-variant tests. Zero behavior change. -->
<!-- reviewed: 2026-04-11 — Post-audit fix bundle. (a) `TokenUsage` and `Timing` gained `Eq, Hash` derives — both are pure integer/DateTime structs, enables dedup and cache keys. `Cost`/`Progress` documented as intentionally `PartialEq`-only (f64 payload breaks reflexivity). (b) `DeferredRetryConfig::validate()` extended to reject `initial_interval == Duration::ZERO` — a zero initial interval with any finite backoff coefficient stays at zero forever and busy-loops the resolver. Five new tests cover the full validation matrix (sane, zero max_attempts, zero interval, NaN/inf/non-positive backoff). (c) `StatefulActionAdapter::execute` state deserialization rewritten from `from_value(state.clone()).or_else(..)` into a `match` — one clone on happy path, a second only when the first deser fails and `migrate_state` is consulted. Semantics unchanged. (d) `POLL_INTERVAL_FLOOR` clamp warning migrated from `tracing::warn!` to `ctx.logger` — last remaining direct `tracing` call in `poll.rs`, closes the Phase 5 / PR 8 migration hole. No throttle field added: the clamp fires at most once per `start()` call. (e) `TestContextBuilder` doc block was accidentally clipped onto the private `ResourceFactory` type alias — reordered so the doc lands on the struct, killing the last `missing_docs` warning. (f) Pre-existing clippy lints fixed as part of the pass: `io_other_error` (`std::io::Error::new(ErrorKind::Other, ..)` → `::other(..)`) in two `error.rs` tests; `manual_async_fn` (`fn stop() -> impl Future` → `async fn stop`) on one `testing.rs` trigger stub; `dead_code` on two `derive_action.rs` test structs whose fields exist only to exercise macro expansion. (g) New `tests/type_sizes.rs` — asserts on `mem::size_of` for 18 key public types (ActionResult<Value>=216, ActionOutput<Value>=144, ActionContext=104, ActionHandler=24, OutputEnvelope=416, DeferredOutput=360, …) plus a niche-optimization sanity check for `Option<ActionOutput<T>>`. Regression guard: future size growth needs either a PR justification or an explicit baseline bump. -->
<!-- reviewed: 2026-04-11 — Workspace-wide nightly rustfmt pass applied (group_imports = "StdExternalCrate", imports_granularity = "Crate", wrap_comments, format_code_in_doc_comments). Touches every Rust file in the crate; purely formatting, zero behavior change. -->
