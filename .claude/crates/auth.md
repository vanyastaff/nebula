# nebula-auth
Authentication and authorization primitives for Nebula.

## Invariants
- Currently a stub — `src/lib.rs` contains only the crate docstring. No types yet.

## Traps
- Do not assume any API exists here. Check `src/` before referencing any types.

## Relations
- Planned to support nebula-api authentication (JWT validation, session management).
