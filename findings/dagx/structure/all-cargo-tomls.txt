# Root Cargo.toml
[workspace]
members = ["dagx-macros", "dagx-test"]

[package]
name = "dagx"
version = "0.3.1"
edition = "2021"
rust-version = "1.81"
authors = ["Stephen Waits <steve@waits.net>"]
license = "MIT"
description = "A minimal, type-safe, runtime-agnostic async DAG (Directed Acyclic Graph) executor with compile-time cycle prevention and true parallel execution"
repository = "https://github.com/swaits/dagx"

[features]
default = ["derive"]
derive = ["dep:dagx-macros"]
tracing = ["dep:tracing"]

[dependencies]
futures-util = { version = "0.3", default-features = false, features = ["std"] }
dagx-macros = { version = "0.3.1", path = "./dagx-macros", optional = true }
tracing = { version = "0.1", optional = true }

# dagx-macros/Cargo.toml
[package]
name = "dagx-macros"
version = "0.3.1"
proc-macro = true
[dependencies]
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"

# dagx-test/Cargo.toml (dev helper, not published)
[package]
name = "dagx-test"
