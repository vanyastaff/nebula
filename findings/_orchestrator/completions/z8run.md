# Completion — z8run — Tier 1

- timestamp: 2026-04-26T14:00:00Z
- word_count: 6860
- key_finding: z8run is the only Rust workflow engine with 10 built-in AI nodes (LLM streaming, embeddings, vector store, multi-turn AI agent, structured output, conversation memory, prompt template, text splitter, classifier, summarizer) plus a working WASM plugin sandbox via wasmtime v42 and single-binary deployment (SQLite dev / PostgreSQL prod). Major gaps: no checkpoint/recovery after crash (in-memory `VectorStoreMap` lost on restart, no `ExecutionRecord.node_logs` filled), broken cron scheduler (process() returns payload but no periodic timer fires it), unenforced WASM capabilities (capabilities declared in manifest but Linker has no WASI imports linked), `reqwest::Client::new()` per `process()` call (no connection pooling), and a vault security gap (credentials table has no user_id — any authenticated user can read any credential in multi-user deployment).
- gaps:
  - A6 (resilience) — confirmed absent; only per-node `timeout_ms` field exists, no retry/circuit-breaker/bulkhead
  - A13 (deployment modes) — single binary only (SQLite or PostgreSQL); no distributed/cluster mode, no multi-node scheduler
  - A15 (observability) — no OTel, no tracing spans exported; only `tracing` macros to local log; `ExecutionRecord.node_logs` is `serde_json::Value` placeholder, always empty
  - A7 (expression DSL) — none; dot-notation in switch/filter is internal only; no Nebula-style `$nodes.foo.result.email`
- escalations:
  - DeepWiki not indexed — z8run/z8run not indexed at deepwiki.com; all 3 queries sent returned "Repository not found"; remaining 6 queries not sent (same error guaranteed). All architecture questions answered from direct code reading.
  - 0 closed GitHub issues — confirmed via `gh issue list --state closed` (empty); 4 "open issues" are all Dependabot PRs. Threshold check: 0 < 100 closed, so ≥3 citation rule does not apply.
- artifacts:
  - architecture.md: findings/z8run/architecture.md
  - structure-summary.md: findings/z8run/structure-summary.md
  - issues-architectural.md: findings/z8run/issues-architectural.md
  - deepwiki-findings.md: findings/z8run/deepwiki-findings.md (3 queries sent, all returned "repo not found")
  - docs/: findings/z8run/docs/ (README, CHANGELOG, CONTRIBUTING, SECURITY)
  - structure/: findings/z8run/structure/ (tree-d3.txt, all-cargo-tomls.txt, git-log.txt)
  - issues count: 0 (threshold: N/A — 0 closed issues)
  - deepwiki queries: 3/9 sent (all failed — repository not indexed)
  - context7 queries: 0 (z8run not a mature crates.io project — 86 downloads; criterion not met)
