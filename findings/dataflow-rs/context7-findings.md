# Context7 Findings — dataflow-rs

## Lookup attempt

`mcp__context7__resolve-library-id libraryName="dataflow-rs"` — no match. GoPlasmatic/dataflow-rs is not indexed in Context7.

Closest results returned were unrelated projects (opendcai/dataflow, timelydataflow/differential-dataflow). Neither is GoPlasmatic/dataflow-rs.

## Status

Not applicable. dataflow-rs is not indexed in Context7 as of this analysis (2026-04-26).

Note: Protocol §3.7 states "only if mature crate >5K downloads on crates.io". dataflow-rs has 22.8K total / 3.4K recent downloads, so the threshold was met, but Context7 does not have the library indexed.
