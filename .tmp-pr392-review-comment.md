## Automated review (Cursor / code-reviewer)

**Verdict: approve** — поведение совпадает с заявленным контрактом; маппинг Postgres соответствует текущей схеме; тестов для объёма изменения достаточно.

### P0–P3 (уверенность ≥ 80%)

Нет.

### Замечания (ниже порога блокировки)

1. **Код `23503` в `map_journal_err`** — для текущего `INSERT INTO execution_journal (execution_id, entry)` единственный FK — это `execution_id → executions(id)`, поэтому `23503` однозначно трактуется как «нет такого execution». Если позже появятся другие FK на `execution_journal`, маппинг может стать неоднозначным; по желанию — однострочный комментарий у `map_journal_err`.

2. **In-memory проверка** — `state` или `workflows` согласованы с `create` и не дают ложного `not_found` в узком окне.

3. **`drop((state, workflows))` перед `journal.write()`** — порядок замков разумный, лишних инверсий с остальным типом нет.

### Пробелы в тестах (опционально)

- Интеграционный тест на реальный FK с `PgExecutionRepo` → `NotFound` (если так принято в проекте).
- Симметрия контракта: `get_journal` по-прежнему возвращает пустой `Vec` для неизвестного id, а `append_journal` — `NotFound`; если это сознатель > стоит одна фраза в доке трейта.

---

*Сгенерировано в Cursor по диффу PR #392.*
