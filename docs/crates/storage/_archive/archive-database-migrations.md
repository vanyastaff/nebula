# Archived From "docs/archive/database-migrations.md"

Database migrations conventions. See docs/database-migrations.md in repo root for current migration docs.

Per-crate: nebula-storage uses repo-root migrations/; nebula-credential uses repo-root migrations/.
