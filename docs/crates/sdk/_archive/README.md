# Archive

Legacy and historical docs for **nebula-sdk** are preserved as follows:

- **Parent directory:** `archive-*.md` and `from-core-full/` in `docs/crates/sdk/` contain archived notes, phase-4 DX drafts, and full-doc extracts. They are kept intact per SPEC (preserve history without loss).
- **Use:** CONSTITUTION Production Vision and DECISIONS may reference these with "From the archives: {filename}".
- **Rules:** Do not delete archived material unless explicitly requested; keep filenames descriptive; reference from active docs when needed.
