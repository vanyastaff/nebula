# Archive

Legacy and historical docs for **nebula-macros** are preserved as follows:

- **Parent directory:** `docs/crates/macros/` contains `archive-*.md`, `from-archive/`, and `from-core-full/` with archived notes, nebula-derive design, and phase-1/core full-doc extracts. They are kept intact per SPEC (preserve history without loss).
- **Use:** CONSTITUTION Production Vision references archive insights (e.g. nebula-derive, parameter/action derive, attribute sets).
- **Rules:** Do not delete source material unless explicitly requested; keep filenames descriptive; reference from active docs when needed.
