# Crate fact sheets (2026-06-12)

One verified, file:line-grounded fact sheet per workspace crate, gathered during the
post-ADR-0092 credential/resource reconciliation. Each is the provenance behind that
crate's `crates/<name>/docs/DESIGN.md` (purpose, public surface, deps/dependents,
module structure, **known tensions/debt**, role in the redesign).

These are a point-in-time audit snapshot, not living docs — when a crate's reality
drifts, fix its `DESIGN.md`, not this sheet. 25 crates covered.
