//! Placeholder crate root for the workspace-level `examples/` member.
//!
//! Runnable demonstrations live in `examples/<topic>_<name>.rs` and are
//! invoked via `cargo run -p nebula-examples --example <name>`. There is
//! intentionally no public API here — this lib only exists so cargo treats
//! the directory as a workspace member.
