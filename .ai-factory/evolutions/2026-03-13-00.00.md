# Evolution: 2026-03-13 00:00

## Intelligence Summary
- Patches analyzed: 0 (first run — no `.ai-factory/patches/` directory)
- Recurring patterns: none (no patches)
- Tech stack: Rust workspace (edition 2024, MSRV 1.93), Axum + Tokio, PostgreSQL, Tauri v2 + React/TypeScript
- Analysis driven by: commitlint config, clippy.toml, rustfmt.toml, CI workflows, AGENTS.md conventions, codebase lib.rs patterns

## Improvements Applied

### aif-commit → skill-context
- Added "Nebula Allowed Commit Types" rule ← driven by: `commitlint.config.cjs` convention
  **File:** `.ai-factory/skill-context/aif-commit/SKILL.md`

### aif-verify → skill-context
- Added "Nebula CI Gate Sequence" rule (5-step sequence with `-D warnings`) ← driven by: `.github/workflows/ci.yml`, `AGENTS.md`
  **File:** `.ai-factory/skill-context/aif-verify/SKILL.md`

### aif-review → skill-context
- Added "Rust Lint Attributes on New Crates" rule ← driven by: `AGENTS.md`, lib.rs patterns
- Added "Lint Suppression Pattern" rule (`#[expect]` not `#[allow]`) ← driven by: `AGENTS.md`
- Added "Wildcard Imports + Error Type Completeness + No anyhow" rule ← driven by: `clippy.toml`, `AGENTS.md`
- Added "Layer Dependency Direction" rule ← driven by: `AGENTS.md` (ADR-003), `deny.toml`
  **File:** `.ai-factory/skill-context/aif-review/SKILL.md`

### aif-implement → skill-context
- Added "Error Enum Required Pattern" rule ← driven by: `AGENTS.md`, `crates/action/src/error.rs`
- Added "New Crate lib.rs Template" rule ← driven by: `AGENTS.md`, codebase patterns
- Added "Lint Suppression Style" rule ← driven by: `AGENTS.md`
- Added "Async Trait Pattern" rule (RPITIT vs async-trait) ← driven by: `AGENTS.md`
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`

### aif-plan → skill-context
- Added "New Crate Workflow" rule ← driven by: `AGENTS.md`, root `Cargo.toml`
  **File:** `.ai-factory/skill-context/aif-plan/SKILL.md`

### aif-ci → skill-context
- Added "Nebula CI Full Structure" rule (audit mode awareness) ← driven by: `.github/workflows/ci.yml` and 5 other workflows
  **File:** `.ai-factory/skill-context/aif-ci/SKILL.md`

### aif-best-practices → skill-context
- Added "Rust Error Handling Pattern" rule ← driven by: `AGENTS.md`, codebase error patterns
- Added "Rust Module and Naming Conventions" rule ← driven by: `AGENTS.md`, `clippy.toml`
  **File:** `.ai-factory/skill-context/aif-best-practices/SKILL.md`

### nebula-layer-dependency-guard → SKILL.md (custom skill, direct edit)
- Added concrete layer table with all 25+ crates mapped to their layers ← driven by: `AGENTS.md`, `CLAUDE.md`
- Added cross-cutting crates "safe at any layer" list
- Enhanced Block Conditions with business→execution/API rule
  **File:** `.agents/skills/nebula-layer-dependency-guard/SKILL.md`

## Patterns Identified
- commitlint type restriction: 1 occurrence (high uniqueness — aif-commit would get this wrong without the rule)
- Rust lint attribute requirements: enforced in every lib crate (systematic)
- Layer dependency enforcement: 25+ crates, enforced by deny.toml (critical architectural guard)
- CI gate completeness: 5-step sequence with `-D warnings` and `--no-deps` doc check (easily missed)
- `#[expect]` vs `#[allow]`: project-wide convention, not default AI behavior
