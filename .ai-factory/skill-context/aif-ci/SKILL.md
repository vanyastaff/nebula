# Project Rules for /aif-ci

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-03-13 00:00
> Based on: 0 analyzed patches (tech-stack + convention analysis)

## Rules

### Nebula CI Full Structure
**Source**: `.github/workflows/ci.yml`, `.github/workflows/miri.yml`, `.github/workflows/security-audit.yml`, `deny.toml`
**Rule**: Nebula already has a complete CI setup in `.github/workflows/`. When running in audit or enhance mode:

**Primary CI workflow (`ci.yml`) has 5 jobs:**
- `fmt`: `cargo fmt --all -- --check`
- `clippy`: `cargo clippy --workspace -- -D warnings`
- `check`: `cargo check --workspace --all-targets`
- `test`: `cargo test --workspace`
- `doc`: `cargo doc --no-deps --workspace`

**Additional workflows to verify are present and correct:**
- `miri.yml` — Miri UB checks (run separately from standard CI)
- `security-audit.yml` — `cargo audit` for dependency vulnerabilities
- `pr-validation.yml` — PR-specific gates
- `test-matrix.yml` — matrix builds across platforms/versions

**Infrastructure standards to check in all jobs:**
- `uses: Swatinem/rust-cache@v2` — must be present in every job for caching
- `env: CARGO_TERM_COLOR: always` — must be set at workflow level
- `concurrency.cancel-in-progress: true` — must be configured for PRs to cancel stale runs
- `uses: dtolnay/rust-toolchain@stable` — standard toolchain setup

**Commit type reminder:** CI config changes must use `chore` type (not `ci`) per `commitlint.config.cjs`.
