[package]
name = "spike-compile-fail"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false
license.workspace = true
description = "Spike — compile-fail probes (iter-3). 4 probes per Gate 3 §15.12.3(e)."

[dependencies]
credential-proto = { path = "../credential-proto" }
credential-proto-builtin = { path = "../credential-proto-builtin" }
zeroize.workspace = true
serde.workspace = true

[dev-dependencies]
trybuild.workspace = true

[[test]]
name = "compile_fail"
path = "tests/compile_fail.rs"

[lints.rust]
unsafe_code = "forbid"
