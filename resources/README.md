# Resources

This directory contains high-level resource specifications and documentation for the Nebula platform.

- `HttpResource` — shared HTTP client resource, implemented in the `nebula-resource` crate.

