apiVersion: v1
kind: Service
metadata:
  name: nebula-api
  labels:
    app.kubernetes.io/name: nebula-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nebula-api
  ports:
    - name: http
      port: 80
      targetPort: http

