refactor(action): remove simple ActionRegistry

Remove the simple HashMap-based ActionRegistry from nebula-action as it's
superseded by the more advanced DashMap-based ActionRegistry in nebula-runtime
which provides lock-free concurrent access.

Updated:
- crates/action/src/registry.rs: deleted file
- crates/action/src/lib.rs: removed module and re-export
- crates/action/src/action.rs: added ActionComponents import
