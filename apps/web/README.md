# Nebula Web

Web frontend app (browser runtime).

Planned role:
- Primary UI for SaaS and self-hosted deployments.
- Shared UI codebase reused by desktop shell where possible.

Next step:
- Initialize frontend stack (React/Vite or preferred framework).
